import styled from 'styled-components';

/*
 * + geosuggest
 * + geosuggest--loading (implementation for this className was not found)
 * */
const GeoSuggestWrapper = styled.div`
  position: relative;
  margin: 1rem auto;
  height: 4.2rem;
  width: 80%;
  font-size: 1.6rem;
  text-align: left;
`
export default GeoSuggestWrapper;
